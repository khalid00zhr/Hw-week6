export interface Ipeople {
    name: string,
    id: string,

  }
  
  export interface Igrade {
    id: string,
    grade: string,
  }
  
  export interface ITask {
    id: string,
    title: string,
    description: string,
    status: boolean,
    
  
  }